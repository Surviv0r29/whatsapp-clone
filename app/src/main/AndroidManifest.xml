<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.google.whatsappclone">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main.Contacts.ContactsActivity"></activity>
        <activity android:name=".Main.chats.ChatsActivity" />
        <activity android:name=".Main.Auth.UserDataActivity" />

        <meta-data
            android:name="androidcheck.googleapis.com "
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.gms.auth.api"
            android:value="AIzaSyAOb3_OyU8L-vo9l2z6kHpP-TM0ZhL_YDc" />

        <activity android:name=".Main.Auth.PhoneLoginActivity" />
        <activity android:name=".Main.Start.WelcomeScreenActivity" />
        <activity android:name=".Main.Start.splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main.MainActivity" />
    </application>

</manifest>